# application-local.yml

# JWT 설정 (로컬 테스트용)
jwt:
  secret: your-super-secret-key-that-is-long-and-secure-enough-to-be-safe
  expiration_time: 3600000

spring:
  # 로컬 DB 접속 정보
  datasource:
    url: jdbc:mysql://localhost:3306/petplace_local?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ssafy # 본인 로컬 DB 비밀번호

  # JPA 설정
  jpa:
    hibernate:
      # 앱 실행 시마다 테이블을 자동으로 삭제하고 다시 생성 (테스트에 유용)
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

